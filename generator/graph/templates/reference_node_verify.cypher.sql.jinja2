{# templates/reference_node_verify.sql.jinja2 #}
-- Verification queries for {{ node_name_pascal }} nodes
SELECT count(*) FROM cypher('{{ project_name }}', $$
    MATCH (n:{{ node_name_pascal }})
    RETURN count(n)
$$) as (count agtype);